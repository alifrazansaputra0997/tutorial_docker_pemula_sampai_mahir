# docker compose down, untuk menghapus semua container, network, dan volume yang digunakan oleh container
version: "3.8"

services:
  nginx-example:
    container_name: nginx-example
    image: nginx:latest
  nginx-example2:
    container_name: nginx-example2
    image: nginx:latest